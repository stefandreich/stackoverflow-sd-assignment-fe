import { AnswerVoteCounter } from './answer-vote-counter';

describe('AnswerVoteCounter', () => {
  it('should create an instance', () => {
    expect(new AnswerVoteCounter()).toBeTruthy();
  });
});
