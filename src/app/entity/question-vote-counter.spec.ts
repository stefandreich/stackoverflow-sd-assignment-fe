import { QuestionVoteCounter } from './question-vote-counter';

describe('QuestionVoteCounter', () => {
  it('should create an instance', () => {
    expect(new QuestionVoteCounter()).toBeTruthy();
  });
});
